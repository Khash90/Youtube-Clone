import { useState } from "react";
import { useNavigate } from "react-router-dom";
import {Paper , IconButton} from '@mui/material';
import { SearcIcon } from '@mui/icons-material';

const SearchBar = () => {
  return (
    <Paper>SearchBar</Paper>
  )
}

export default SearchBar